{% extends 'base.html.twig' %}

{% block title %}ServicedObject{% endblock %}

{% block body %}
    <h1>ServicedObject</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ serviced_object.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ serviced_object.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_serviced_object_index') }}">back to list</a>

    <a href="{{ path('app_serviced_object_edit', {'id': serviced_object.id}) }}">edit</a>

    {{ include('serviced_object/_delete_form.html.twig') }}
{% endblock %}
